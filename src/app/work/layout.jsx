export const metadata = {
  title: "Our Work",
  description: "Explore our portfolio of digital products, web applications, and transformation projects for forward-thinking companies.",
};

export default function WorkLayout({ children }) {
  return children;
}
