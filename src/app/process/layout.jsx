export const metadata = {
  title: "Our Process",
  description: "From discovery to delivery, our proven methodology ensures we build the right product the right way.",
};

export default function ProcessLayout({ children }) {
  return children;
}
